# Development Environment Helm Secrets (SOPS Encrypted)
# Environment-specific sensitive values for development
# To decrypt: sops -d secrets.yaml
# To edit: sops secrets.yaml

# Database Configuration
database:
  password: "dev-db-password-123"
  
# Application Secrets
backend:
  jwtSecret: "dev-jwt-secret-2024"
  
# RabbitMQ Configuration
rabbitmq:
  auth:
    password: "dev-rabbitmq-pass-456"

# Development-specific API Keys
monitoring:
  apiKey: "dev-monitoring-key-789"

# Note: This is a template - actual file should be encrypted with SOPS
# Run: sops -e -i secrets.yaml to encrypt this file